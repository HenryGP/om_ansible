sudo: required

notifications:
  email: false

services:
  - docker

before_install:
  - docker-compose build
  - docker-compose up -d

script:
  - docker exec -it omansible_provisioner_1 /opt/ansible-2.3.1.0/bin/ansible-playbook /root/om_ansible.yaml
