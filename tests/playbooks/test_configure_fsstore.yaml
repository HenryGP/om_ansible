- name: "Creating FSStore in Ops Manager"
  hosts: localhost
  become: yes
  gather_facts: no
  ignore_errors: no
  vars:
    api_key: "{{ lookup('file', '/root/admin_user_api_key.yaml') }}"
  tasks:
    - include: ../../tasks/api-create-fs-store.yaml